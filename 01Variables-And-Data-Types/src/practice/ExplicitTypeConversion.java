package practice;

public class ExplicitTypeConversion {
    public static void main(String[] args) {
        long a  = 89;   // a is long
        int b = (int) a; // b is int
        System.out.println(a); //java doesnt support this type of things so we have to explicitly cast  in int (int)
    }
}
